class RedirectHelper {
    static redirect() {
        const REDIRECT_URL = `${window.location.origin}/pavelhalanin-JSFEPRESCHOOL2026Q1/christmas-shop/home/`;
        window.location.replace(REDIRECT_URL);
    }
}